services:
  quiz:
    build: .
    container_name: valdoise-quiz
    restart: unless-stopped
    ports:
      - "3012:3012"
    volumes:
      - quiz-data:/app/quiz.db
    environment:
      - PORT=3012
      - NODE_ENV=production

volumes:
  quiz-data:
